function sumAndAverage (mathematics, physics,english){
    const sum = mathematics + physics + english;
    const average = sum / 3;
    console.log(`Sum: ${sum}`);
    console.log(`Average: ${average}`);
}

sumAndAverage(1,2,4);